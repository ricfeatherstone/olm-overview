apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: demo-operator
spec:
  name: demo-operator
  source: rics-operators
  sourceNamespace: olm
  installPlanApproval: Automatic
  config:
    resources:
      requests:
        cpu: 50m
        memory: 10Mi
      limits:
        cpu: 100m
        memory: 50Mi
